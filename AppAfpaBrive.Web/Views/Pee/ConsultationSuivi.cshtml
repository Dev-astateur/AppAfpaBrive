@model AppAfpaBrive.BOL.PeeDocument
@using AppAfpaBrive.BOL;
@{
    List<PeeDocument> listPeeDocument = new List<PeeDocument>();
    listPeeDocument = ViewData["PeeDocument"] as List<PeeDocument>;
    var listItem = listPeeDocument.Select(P => new SelectListItem { Text = P.IdPeeNavigation.MatriculeBeneficiaireNavigation.NomBeneficiaire +" " + P.IdPeeNavigation.MatriculeBeneficiaireNavigation.PrenomBeneficiaire, Value = P.IdPeeDocument.ToString() });
    var listPth = listPeeDocument.Select(P => new SelectListItem { Text = P.PathDocument });

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>View</title>
</head>
<body>

    <h4>PeeDocument</h4>
    <hr />
    <div class="row">
        <div class="col-sm-12 col-md-12 col-lg-12">
            <form asp-action="ConsultationSuivi">
                @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="IdPeeDocument" />*@
                <div class="form-group col-sm-12 col-md-8 col-lg-8">
                    @if (listItem != null)
                    {
                        @Html.DropDownListFor(m => m.IdPeeDocument, listItem, ".....Seléctionnez le Stagiaire........", new { @class = "form-control" })
                    }
                </div>
                <div class="form-group col-sm-12 col-md-8 col-lg-8">

                </div>
            </form>
            <div class="form-group col-sm-12 col-md-8 col-lg-8">
                <a id="DownLoadDoc" class="btn btn-primary btn-block " href="">Télécharger le document</a>
            </div>
        </div>
    </div>


    <div>
        <a asp-action="Index">Back to List</a>
    </div>

</body>

</html>
@section Scripts{
    <script type="text/javascript">

        $(document).ready(function () {
            $("#IdPeeDocument").on('change', function () {
                
                var IdPeeDocument = $(this).val();
                var pathDoc = './DownLoadDocument?IdPeeDoc=' + IdPeeDocument;
                $("#DownLoadDoc").attr("href",pathDoc)

                });
                
               

        });

    </script>
}
