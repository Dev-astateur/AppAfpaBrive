@model IEnumerable<AppAfpaBrive.BOL.OffreFormation>
@*@model IEnumerable<AppAfpaBrive.Web.ModelView.OffreDeFormation>*@

@{
    ViewData["Title"] = "ListeOffreFormation";
    //List<AppAfpaBrive.BOL.OffreFormation> listeOffreFormations = ViewBag.MaListe;
    //var listeItemOffreFormation = listeOffreFormations.Select(o => new SelectListItem() { Text = o.LibelleOffreFormation, Value = o.IdOffreFormation.ToString()});
}

<h2>Sélectionnez une offre de formation</h2>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LibelleOffreFormation)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.BeneficiaireOffreFormations)
                </th>*@

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                        @Html.DisplayFor(modelItem => item.LibelleOffreFormation)
                    </td>
                @*<td>
                        @Html.DropDownListFor(modelItem => item.LibelleOffreFormation, new SelectList(Model.LibelleOffreFormation, "Value", "Text"), htmlAttributes: new { @class = "form-control", id = "OffreDeFormation" })
                        @Html.ValidationMessageFor(x => x.LibelleOffreFormation, "", new { @class = "text-danger" });
                    </td>*@
                <div class>

                </div>
                <td>
                    <div class="form-group">
                        <a type="submit" value="Select" class="btn btn-primary" asp-route-id="@item.BeneficiaireOffreFormations" asp-controller="EditerInfosStagiaire2" asp-action="Edit">Séléctionner</a>
                        <a type="submit" value="Menu" class="btn btn-danger" asp-controller="EditerInfosStagiaire2" asp-action="Edit">Revenir au menu principal</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
