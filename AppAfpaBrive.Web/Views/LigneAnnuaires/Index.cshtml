@model IEnumerable<AppAfpaBrive.BOL.AnnuaireSocial.LigneAnnuaire>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PublicConcerne)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ServiceAbrege)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Service)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conditions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Structure)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PublicConcerne)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceAbrege)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Service)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conditions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Structure.IdStructure)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLigneAnnuaire">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLigneAnnuaire">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLigneAnnuaire">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
